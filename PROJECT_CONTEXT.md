# سياق مشروع لوحة الفريج

هذا الملف مخصص لأي إيجنت أو مطور يعمل لاحقًا على المشروع حتى يفهم المقصود بدون مراجعة كل الملفات من الصفر.

## الهدف

الموقع يعرض لوحة داخلية مبسطة لإعلانات شركة عبدالعزيز سعود الفريج العقارية. الهدف العملي هو أن يرى الموظف الإعلانات حسب المحافظة والمنطقة ونوع الحركة ونوع العقار، ويفتح الإعلان الأصلي عند الحاجة.

الصفحة الأساسية المنشورة:

- GitHub Pages: `https://ahmedkamalsa.github.io/alforaijboard/`
- Netlify: `https://alforaijboard.netlify.app/`

## مصدر البيانات

المصدر الأساسي هو بيانات الفريج التي يجلبها سكربت:

```powershell
python agent\refresh_dashboard_register.py
```

ثم يتم بناء الموقع الثابت عبر:

```powershell
python agent\build_static_site.py
python agent\validate_static_site.py
```

التدفق المختصر:

- `agent/build_director_manager_report.py`: يجلب وينظف البيانات ويولد سجل Excel وسيط.
- `agent/build_offer_evidence_browser.py`: يحول السجل إلى لوحة HTML وExcel تفصيلي.
- `agent/build_static_site.py`: ينسخ النسخة النهائية إلى مجلد `site`.
- `.github/workflows/update-dashboard.yml`: يبني الموقع وينشر `gh-pages`.

## قاعدة مهمة للمساحة

لا تعتبر أي رقم مثل `ارتداد 40 متر` مساحة للعقار. المساحة تعتمد فقط على:

- حقل المساحة الصريح من الـ API، أو
- نص صريح مثل `المساحة: 300م²`.

إذا لم توجد مساحة صريحة تظهر للموظف كـ `غير محدد` فقط، بدون عرض سبب داخلي أو ملاحظات جودة.

مثال مهم: إعلان `AF-303` يحتوي `ارتداد 40 متر`، لذلك يجب أن تكون المساحة `غير محدد`، وليس `40 م²`.

## قواعد واجهة الموظف

- لا تعرض ملاحظات جودة البيانات داخل الكروت أو تفاصيل الإعلان.
- لا تعرض `مصدر المساحة` أو `مصدر السعر` داخل الكروت أو المودال.
- لا تعرض زر `تقرير جودة البيانات`.
- الواجهة يجب أن تبقى بسيطة للموظف: الكود، المحافظة، المنطقة، نوع العقار، السعر، المساحة، تاريخ النشر، وفتح الإعلان الأصلي.
- عند اختيار كارت مثل عروض للبيع أو عروض الإيجار لا يتم مسح المحافظة/المنطقة الحالية.
- آخر منطقة بحث يكتبها المستخدم يتم حفظها وتظهر أول اختيار عند فتح قائمة المنطقة لاحقًا.

## Excel مقابل CSV

زر Excel يحمل الملف الكامل `downloads/offer-evidence.xlsx`.

- مناسب للإدارة والمراجعة والأرشفة.
- يحتوي كل القوائم والأوراق والتصنيفات.
- قد يحتوي أعمدة تدقيق داخلية مثل مصدر السعر أو مصدر المساحة أو ملاحظات جودة البيانات، لأنها مفيدة للمراجعة وليست للعرض اليومي.

زر CSV يحمل نتائج الاختيار الحالي فقط.

- يعتمد على الفلاتر الحالية في الصفحة.
- مناسب للموظف عندما يريد إرسال أو حفظ نتيجة محددة، مثل محافظة الجهراء + المطلاع + عروض للبيع.
- يجب أن يبقى مختصرًا وبلا أعمدة تدقيق داخلية حتى لا يشتت المستخدم.

الأفضل عمليًا:

- للموظفين: استخدم CSV أو طباعة/حفظ PDF بعد اختيار الفلاتر.
- للإدارة أو مراجعة البيانات: استخدم Excel الكامل.

## الملفات المنشورة

داخل `site` المتوقع وجود:

- `index.html`
- `downloads/offer-evidence.xlsx`
- `assets/`
- `_headers`
- `robots.txt`
- `last-updated.json`

لا تنشر `qa.html` في واجهة الموظف.

## تحقق سريع بعد أي تعديل

بعد البناء يجب التأكد من:

- عدد الإعلانات في الصفحة ليس صفرًا.
- `AF-303` مساحته `null` أو تظهر `غير محدد`.
- لا يوجد زر `تقرير جودة البيانات`.
- لا يوجد نص ظاهر من نوع `مصدر المساحة: غير مذكورة` داخل كروت الإعلان.
- GitHub Actions ينتهي بنجاح وينشر `gh-pages`.
