# نشر لوحة الأرقام كصفحة محدثة تلقائيا

## الاختيار المنفذ

تم تجهيز نشر ثابت بدون قاعدة بيانات في هذه المرحلة:

- `site/index.html`: الصفحة التي تفتح للمدير أو الموظفين.
- `site/downloads/offer-evidence.xlsx`: ملف Excel المساند.
- `agent/refresh_dashboard_register.py`: تحديث خفيف للبيانات من API بدون توليد PDF أو PowerPoint.
- `agent/build_static_site.py`: يعيد بناء اللوحة وينسخها إلى مجلد `site`.
- `netlify.toml`: إعداد Netlify.
- `vercel.json`: إعداد Vercel إذا تم اختيار Vercel بدلا من Netlify.
- `.github/workflows/update-dashboard.yml`: تحديث يومي الساعة 6 صباحا بتوقيت الكويت، مع إمكانية تشغيله يدويا.

## طريقة النشر المقترحة

1. ارفع هذا المجلد إلى مستودع GitHub خاص.
2. اربط المستودع بموقع Netlify.
3. اجعل أمر البناء كما هو في `netlify.toml`.
4. اجعل مجلد النشر `site`.
5. بعد أول نشر، استخدم رابط `index.html` كلوحة متابعة.

## ملاحظات مهمة

- الصفحة لا تحتاج Supabase الآن لأن البيانات مدمجة داخل HTML وقت البناء.
- عند زيادة الاستخدام أو الحاجة إلى صلاحيات دخول وسجل مستخدمين، نرقي إلى قاعدة مثل Supabase.
- تم إضافة `robots.txt` و `_headers` لمنع الفهرسة قدر الإمكان، لكن الرابط العام ليس بديلا عن نظام صلاحيات إذا كانت البيانات حساسة.
